defaultNamespace: opcua-system

targetCustomizations:
  - name: edge-pre
    clusterSelector:
      matchLabels:
        environment: pre
        fleet-environment: edge

  - name: edge-pro
    clusterSelector:
      matchLabels:
        environment: pro
        fleet-environment: edge